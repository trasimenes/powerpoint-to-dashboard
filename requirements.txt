# Recommandé : Python 3.11.x
# Certains paquets ne sont pas encore compatibles avec Python 3.13+
# Installation : pyenv local 3.11.9 && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt

Flask==3.0.0
python-pptx==0.6.23
Werkzeug==3.0.1
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
lxml==5.1.0
Pillow==10.2.0
